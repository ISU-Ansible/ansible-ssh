---
- hosts: localhost
  remote_user: root

  roles:
    - role_under_test

  tasks:
    - name: "Evaluate SSHD"
      shell: service sshd status
      register: sshd_status

    - debug:
        var: sshd_status
        verbosity: 4
